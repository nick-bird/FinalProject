import { SoundboardExpression } from './soundboard-expression';

describe('SoundboardExpression', () => {
  it('should create an instance', () => {
    expect(new SoundboardExpression()).toBeTruthy();
  });
});
